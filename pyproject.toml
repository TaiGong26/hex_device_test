[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hex_device_test"
version = "0.1.0"
description = "HEX device control test"
authors = [{name="TTG"}]
readme = "README.md"
requires-python = ">=3.8"


dependencies = [
    "numpy",
]